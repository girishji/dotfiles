from enum import Enum
from itertools import tee, chain, count, cycle, islice, repeat, batched, groupby, product, starmap, compress, pairwise, dropwhile, takewhile, accumulate, zip_longest, filterfalse, Element, combinations, permutations, combinations_with_replacement, chain, chain_iter
import itertools
import math
from typing import List, Optional
from sys import stderr
import sys
import copy
import collections
from collections import Counter, defaultdict
from heapq import heappush, heappop, heappushpop, heapify, heapreplace, merge, nlargest, nsmallest
import heapq
import bisect
from bisect import bisect_left, bisect_right, bisect, insort, insort_left, insort_right

__forget_imports__ = ["from numpy import NaN"]
__forget_imports__ = ["from numpy import copy"]
__forget_imports__ = ["from scipy.optimize.zeros import bisect"]
__forget_imports__ = ["from numpy import product"]

