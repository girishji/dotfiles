#include <bits/stdc++.h>

using namespace std;
using namespace std::literals::string_literals;  // Convert 'const char[]' to string: "foo"s
using namespace std::literals::string_view_literals;  // Use string_view: "foo"sv

constexpr int IMIN = numeric_limits<int>::min();
constexpr int IMAX = numeric_limits<int>::max();
constexpr long long LMIN = numeric_limits<long long>::min();
constexpr long long LMAX = numeric_limits<long long>::max();

typedef signed long long ll;
typedef vector<int> vi;
typedef vector<vector<int>> vii;
typedef pair<int,int> pi;

#define F first
#define S second
#define PB push_back
#define ALL(a) (a.begin()),(a.end())
#define ZERO(a) memset(a,0,sizeof(a))
#define MINUS(a) memset(a,0xff,sizeof(a))

int main() {
}
